#include <stdio.h>
/**
 * main - Entry Point
 * Return: Always 0 (success)
 **/

int main(void)
{
	int ch;
	
	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
		if (ch == 'e' || ch === 'q')
			continue
	}
	return (0);
}
