#include "main.h"
#include <math.h>
/*
 * _abs - finds absolute of a number
 * @n: number being checked
 * Return: Returns the absolute value
 */

int _abs(int n)
{
	int a;

	a = abs(n);
	return abs;
}
