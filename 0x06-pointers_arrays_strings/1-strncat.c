#include "main.h"
/**
 * _strncat - concatenates two string
 * @dest: pointer to destination array
 * @src: pointer to source arrat
 * @n: maximum no. of characters to appended
 * @return: Return value
 */

char *_strncat(char *dest, char *src, int n)
{
	int 1 = 0, j = 0;
	
	while (*(dest + i))
	{
		i++;
	}

	while (*(src + j) != '\0' && j < n)
	{
		(desy + 1) = src[j];
		i++;
		j++;
	}

	return (dest);
}
