#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: number to be multiplied.
 * @b: number to be multiplied.
 * Return: Retuen value
 */

int mul(int a, int b)
{
	int prod;

	prod = (a * b);

	return (prod);
}
